import React from 'react';
import ChatPage from '../chat/ChatPage';

/**
 * Mentor-specific wrapper for ChatPage.
 * Can be customized in the future if mentors need different features 
 * (e.g., sending quick replies, sharing resources, etc.)
 */
const MentorChatPage = () => {
  return <ChatPage />;
};

export default MentorChatPage;